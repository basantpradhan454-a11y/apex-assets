# Streamlit Cloud Secrets — copy this content into your app's "Secrets" section
# at https://share.streamlit.io → your app → Settings → Secrets
# DO NOT commit the real secrets.toml to GitHub

DATABASE_URL = "postgresql://user:password@host:5432/dbname"
